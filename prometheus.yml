global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "california-housing-api"
    metrics_path: "/prometheus-metrics"
    static_configs:
      - targets: ["api:8000"]
